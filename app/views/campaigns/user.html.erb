<div id="contForm">
<div class="container">

<%= form_tag (campaign_update_user_path(@camp)) do %>
 <table class="table">
  <tr>
    <th>chech</th>
    <th></th>
    <th>Nombre</th>
    <th>Precio</th>
    <th>Categoria</th>
  </tr>
  
  <% @user.each do |user| %>
  <tr>
    <td><%= radio_button_tag(:user, user.id) %></td>
    <td><%= image_tag user.image%></td>
    <td><%= user.name %></td>
    <td></td>
    
  </tr>
  <% end -%>


  <% end %>

</table>
 <%= submit_tag  %>
</div>
</div>